export function Inter(_options?: { subsets?: string[]; variable?: string }) {
  return {
    className: "font-sans",
    variable: "--font-sans",
  }
}

export function Outfit(_options?: { subsets?: string[]; variable?: string }) {
  return {
    className: "font-outfit",
    variable: "--font-outfit",
  }
}
